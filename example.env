# Copy to .env and fill in values.

# Used for contract fetching and (for supported chains) read-only eth_call via Etherscan proxy.
ETHERSCAN_API_KEY=

# Read-only contract calls (eth_call) via direct JSON-RPC, required for these chains because
# the free Etherscan API does not support chain ids:
# BNB Smart Chain Mainnet (56), BNB Smart Chain Testnet (97),
# Base Mainnet (8453), Base Sepolia (84532),
# OP Mainnet (10), OP Sepolia (11155420),
# Avalanche C-Chain (43114), Avalanche Fuji (43113).

RPC_URL_56=
RPC_URL_97=
RPC_URL_8453=
RPC_URL_84532=
RPC_URL_10=
RPC_URL_11155420=
RPC_URL_43114=
RPC_URL_43113=

